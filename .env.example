# Optional — only needed if NOT using subscription auth via ~/.claude
# ANTHROPIC_API_KEY=sk-ant-...

# Browse root for the directory browser (set automatically in Docker Compose)
# YOLO_BROWSE_ROOT=/host/home
